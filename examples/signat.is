let unit : * c1
	= |A : * c0| -> A -> A;

let list : nat -> * c1
	= |n| n :: |n| * c1 {
		| 0       -> unit
		| suc n T -> T & nat
	};

let make_list : |n : nat| -> list n
	= |n| n :: |n| list n {
		| 0       -> |A| |a| a
		| suc n k -> k, n
	};

make_list 10
