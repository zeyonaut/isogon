let! list : nat -> * c1 rpointer
	= |n| n :: |_| * c1 rpointer {
		| 0        -> RC #1
		| suc @_.T -> RC (T & nat)
	};

let! make_list : |n : nat| -> list n
	= |n| n :: |n| list n {
		| 0        -> rc 0_1
		| suc @n.k -> rc (k, n)
	};

make_list 10
