let! T : bool -> * c1 rpointer
	= |b| b :: |_| (* c1 rpointer) {
		| false -> RC (nat  & bool)
		| true  -> RC (bool & nat)
	};

let! test : |b : bool| -> T b
	= |b| b :: |x| T x {
		| false -> rc (0, false)
		| true  -> rc (true, 1)
	};

test false unrc
