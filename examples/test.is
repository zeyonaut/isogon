let A : * c1 = |PA : * c0| -> PA;
def B : '* c1 -> '* c1 = |T| [A -> [T]];
let C : * c0 = |PC : * c0| & PC;
let D : * c0 = * c0 & * c0;
let x : |T : * c0| & * c0 -> * c0 -> * c1 = * c0, (|T| |K| A & T & K);

[B [x/! (x/.) C]]
