let add : nat -> nat -> nat
	= |a| |b| a :: |n| nat {
		| 0        -> b
		| suc @n.k -> suc k
	};

let fib2 : nat -> (nat & nat) -> (nat & nat)
	= |n| |x| n :: |n| (nat & nat) {
		| 0        -> x
		| suc @n.x -> add (x/.) (x/!), x/.
	};

let fib : nat -> nat
	= |n| fib2 n (0, 1)/.;

fib 14
